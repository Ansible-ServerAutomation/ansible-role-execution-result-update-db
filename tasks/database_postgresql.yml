---
# tasks/database_postgresql.yml - PostgreSQL specific tasks

- name: Execute PostgreSQL database operations on localhost
  block:
    - name: Skip PostgreSQL tasks if no prepared records
      ansible.builtin.meta: end_host
      when: _execution_results_prepared | length == 0

    - name: Debug - Show database connection details
      ansible.builtin.debug:
        msg:
          - "Connecting to PostgreSQL database"
          - "Host: {{ execution_result_db_host }}"
          - "Port: {{ execution_result_db_port }}"
          - "Database: {{ execution_result_db_name }}"
          - "User: {{ execution_result_db_user }}"
          - "Password: {{ 'Provided' if execution_result_db_password else 'Not provided' }}"
          - "Tables: {{ execution_result_db_table_success }}, {{ execution_result_db_table_failed }}"
      when: execution_result_debug_mode | bool

    - name: Verify database exists using Python
      ansible.builtin.shell: |
        python3 << 'EOF'
        import psycopg2
        import sys
        try:
            conn = psycopg2.connect(
                host='{{ execution_result_db_host }}',
                port={{ execution_result_db_port }},
                user='{{ execution_result_db_user }}',
                password='{{ execution_result_db_password }}',
                database='postgres',
                connect_timeout=10
            )
            cur = conn.cursor()
            cur.execute('SELECT 1 FROM pg_database WHERE datname = %s', ('{{ execution_result_db_name }}',))
            result = cur.fetchone()
            cur.close()
            conn.close()
            if result:
                print('EXISTS')
                sys.exit(0)
            else:
                print('NOT_FOUND')
                sys.exit(1)
        except Exception as e:
            print(f'ERROR: {e}')
            sys.exit(2)
        EOF
      register: _db_exists_check
      changed_when: false
      failed_when: _db_exists_check.rc == 2
      no_log: true
      run_once: true

    - name: Fail if database does not exist
      ansible.builtin.fail:
        msg: "Database '{{ execution_result_db_name }}' does not exist on {{ execution_result_db_host }}. Please create it before using this role."
      when: _db_exists_check.rc != 0
      run_once: true

    - name: Verify success_jobs table exists using Python
      ansible.builtin.shell: |
        python3 << 'EOF'
        import psycopg2
        import sys
        try:
            conn = psycopg2.connect(
                host='{{ execution_result_db_host }}',
                port={{ execution_result_db_port }},
                user='{{ execution_result_db_user }}',
                password='{{ execution_result_db_password }}',
                database='{{ execution_result_db_name }}',
                connect_timeout=10
            )
            cur = conn.cursor()
            cur.execute('SELECT 1 FROM information_schema.tables WHERE table_name = %s', ('{{ execution_result_db_table_success }}',))
            result = cur.fetchone()
            cur.close()
            conn.close()
            if result:
                print('EXISTS')
                sys.exit(0)
            else:
                print('NOT_FOUND')
                sys.exit(1)
        except Exception as e:
            print(f'ERROR: {e}')
            sys.exit(2)
        EOF
      register: _success_table_exists
      changed_when: false
      failed_when: _success_table_exists.rc == 2
      no_log: true
      run_once: true

    - name: Fail if success_jobs table does not exist
      ansible.builtin.fail:
        msg: "Table '{{ execution_result_db_table_success }}' does not exist in database '{{ execution_result_db_name }}'. Please create it before using this role."
      when: _success_table_exists.rc != 0
      run_once: true

    - name: Verify failed_jobs table exists using Python
      ansible.builtin.shell: |
        python3 << 'EOF'
        import psycopg2
        import sys
        try:
            conn = psycopg2.connect(
                host='{{ execution_result_db_host }}',
                port={{ execution_result_db_port }},
                user='{{ execution_result_db_user }}',
                password='{{ execution_result_db_password }}',
                database='{{ execution_result_db_name }}',
                connect_timeout=10
            )
            cur = conn.cursor()
            cur.execute('SELECT 1 FROM information_schema.tables WHERE table_name = %s', ('{{ execution_result_db_table_failed }}',))
            result = cur.fetchone()
            cur.close()
            conn.close()
            if result:
                print('EXISTS')
                sys.exit(0)
            else:
                print('NOT_FOUND')
                sys.exit(1)
        except Exception as e:
            print(f'ERROR: {e}')
            sys.exit(2)
        EOF
      register: _failed_table_exists
      changed_when: false
      failed_when: _failed_table_exists.rc == 2
      no_log: true
      run_once: true

    - name: Fail if failed_jobs table does not exist
      ansible.builtin.fail:
        msg: "Table '{{ execution_result_db_table_failed }}' does not exist in database '{{ execution_result_db_name }}'. Please create it before using this role."
      when: _failed_table_exists.rc != 0
      run_once: true

    - name: Insert successful execution results into PostgreSQL using Python
      block:
        - name: Insert success records via psycopg2
          ansible.builtin.shell:
            cmd: |
              python3 << 'EOFPYTHON'
              import psycopg2
              import json
              import sys
              
              record = json.loads('''{{ item | to_json }}''')
              
              try:
                  conn = psycopg2.connect(
                      host="{{ execution_result_db_host }}",
                      port={{ execution_result_db_port }},
                      user="{{ execution_result_db_user }}",
                      password="{{ execution_result_db_password }}",
                      database="{{ execution_result_db_name }}",
                      connect_timeout=10
                  )
                  cur = conn.cursor()
                  cur.execute("""
                      INSERT INTO {{ execution_result_db_table_success }} (
                          timestamp, hostname, inventory_hostname, ansible_host,
                          project, organization, job_template, playbook,
                          scm_url, scm_branch, scm_revision, execution_environment,
                          status, return_code, message, stdout, stderr, exception,
                          warnings, failed_task, failed_task_module,
                          os_distribution, os_version, os_family, os_system, os_architecture
                      ) VALUES (
                          %(timestamp)s, %(hostname)s, %(inventory_hostname)s, %(ansible_host)s,
                          %(project)s, %(organization)s, %(job_template)s, %(playbook)s,
                          %(scm_url)s, %(scm_branch)s, %(scm_revision)s, %(execution_environment)s,
                          %(status)s, %(return_code)s, %(message)s, %(stdout)s, %(stderr)s, %(exception)s,
                          %(warnings)s, %(failed_task)s, %(failed_task_module)s,
                          %(os_distribution)s, %(os_version)s, %(os_family)s, %(os_system)s, %(os_architecture)s
                      )
                  """, record)
                  conn.commit()
                  cur.close()
                  conn.close()
                  print("SUCCESS")
              except Exception as e:
                  print(f"ERROR: {e}")
                  sys.exit(1)
              EOFPYTHON
          loop: "{{ _execution_results_success }}"
          loop_control:
            label: "{{ item.timestamp }} - {{ item.status }}"
          register: _insert_success_result
          changed_when: true
          ignore_errors: "{{ not execution_result_fail_on_db_error }}"
          no_log: true
      when:
        - execution_result_batch_insert | bool
        - _execution_results_success | length > 0

    - name: Insert failed execution results into PostgreSQL using Python
      block:
        - name: Insert failed records via psycopg2
          ansible.builtin.shell:
            cmd: |
              python3 << 'EOFPYTHON'
              import psycopg2
              import json
              import sys
              
              record = json.loads('''{{ item | to_json }}''')
              
              try:
                  conn = psycopg2.connect(
                      host="{{ execution_result_db_host }}",
                      port={{ execution_result_db_port }},
                      user="{{ execution_result_db_user }}",
                      password="{{ execution_result_db_password }}",
                      database="{{ execution_result_db_name }}",
                      connect_timeout=10
                  )
                  cur = conn.cursor()
                  cur.execute("""
                      INSERT INTO {{ execution_result_db_table_failed }} (
                          timestamp, hostname, inventory_hostname, ansible_host,
                          project, organization, job_template, playbook,
                          scm_url, scm_branch, scm_revision, execution_environment,
                          status, return_code, message, stdout, stderr, exception,
                          warnings, failed_task, failed_task_module,
                          os_distribution, os_version, os_family, os_system, os_architecture
                      ) VALUES (
                          %(timestamp)s, %(hostname)s, %(inventory_hostname)s, %(ansible_host)s,
                          %(project)s, %(organization)s, %(job_template)s, %(playbook)s,
                          %(scm_url)s, %(scm_branch)s, %(scm_revision)s, %(execution_environment)s,
                          %(status)s, %(return_code)s, %(message)s, %(stdout)s, %(stderr)s, %(exception)s,
                          %(warnings)s, %(failed_task)s, %(failed_task_module)s,
                          %(os_distribution)s, %(os_version)s, %(os_family)s, %(os_system)s, %(os_architecture)s
                      )
                  """, record)
                  conn.commit()
                  cur.close()
                  conn.close()
                  print("SUCCESS")
              except Exception as e:
                  print(f"ERROR: {e}")
                  sys.exit(1)
              EOFPYTHON
          loop: "{{ _execution_results_failed }}"
          loop_control:
            label: "{{ item.timestamp }} - {{ item.status }}"
          register: _insert_failed_result
          changed_when: true
          ignore_errors: "{{ not execution_result_fail_on_db_error }}"
          no_log: true
      when:
        - execution_result_batch_insert | bool
        - _execution_results_failed | length > 0

    - name: Set records processed count
      ansible.builtin.set_fact:
        _execution_result_records_processed: "{{ (_execution_results_success | length) + (_execution_results_failed | length) }}"
  delegate_to: localhost

- name: Debug - Show insert results
  ansible.builtin.debug:
    msg: "Successfully inserted {{ _execution_results_success | length }} success record(s) and {{ _execution_results_failed | length }} failed record(s) into PostgreSQL"
  when:
    - execution_result_debug_mode | bool
    - _execution_result_records_processed | int > 0
